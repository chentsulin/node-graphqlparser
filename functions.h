#ifndef NATIVE_EXTENSION_GRAB_H
#define NATIVE_EXTENSION_GRAB_H

#include <nan.h>

// Top-level functions. These functions's implementations are in functions.cc

NAN_METHOD(parseString);
NAN_METHOD(parseFile);

#endif
